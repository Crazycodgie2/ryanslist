import React from "react"
import { useExample } from "../hooks"

export default props => {
  const {} = useExample()

  return (
    <div className="container-left">
      <h2 className="header">ryanslist</h2>
    </div>
  )
}
